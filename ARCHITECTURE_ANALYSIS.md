# CFM 检测器系统架构深度分析与改进方向

> 聚焦核心架构和技术问题。论文写作、评估指标、统计显著性等实验相关问题另行处理。

---

## 目录

1. [CRITICAL 问题](#1-critical-问题)
2. [检测模型与训练](#2-检测模型与训练)
3. [仿真系统与控制](#3-仿真系统与控制)
4. [改进路线图](#4-改进路线图)

---

## 1. CRITICAL 问题

必须最先修复的核心正确性问题。

### C1. EKF 测量噪声协方差 R 矩阵错误

**位置**：`model.py:431`

```python
self.R = np.diag([0.008, 0.008, 0.002])    # 标准差直接当方差用
```

传感器噪声标准差 σ_xy=0.008m, σ_θ=0.004rad。Kalman 滤波器中 R 必须是**方差**：
- 正确的 R_xy = 0.008² = 6.4×10⁻⁵
- 正确的 R_θ = 0.004² = 1.6×10⁻⁵

当前 R 比正确值大 **125 倍**。Kalman 增益 ≈ 0（R >> P），EKF 等价于死推算——完全忽略测量值。所有基于 EKF 的状态估计、创新信号和检测结果均受此影响。

**修复**：`self.R = np.diag([0.008**2, 0.008**2, 0.004**2])`。修正后需重新调谐 Q。

---

### C2. 物理损失施加在错误量上

**位置**：`detector/train_cfm.py:219, 233`

```python
x_t = (1 - t_expanded) * x_0 + t_expanded * atk_target    # OT路径中间态（含高斯噪声）
y_rec_t = y_meas_phys - x_t                                # 用含噪中间态恢复信号
r_phys = compute_physics_residual(y_rec_t, u_cmd_phys)     # 运动学残差
```

物理残差施加在 `y_meas - x_t` 上，其中 `x_t` 是 `x_0 ~ N(0,I)` 和真值的混合。当 `t ≈ 0` 时，`x_t ≈ N(0,I)`（纯噪声），`y_rec_t` 是非物理随机信号，此时物理损失无意义。`t ~ Uniform(0,1)` 意味着约一半的物理损失评估是噪声。

**修复方案**：
- **方案A**（推荐）：用 `t` 加权：`L_phys_weighted = t * L_phys`，使后期流时间占主导
- **方案B**：仅对 ODE 终点（`sample_ode` 输出）计算物理损失
- **方案C**：仅在 `t > 0.5` 时计算物理损失

---

### C3. 推理时骨干网络运行两次

**位置**：`cfm_backend.py:342-344 (_nn_infer)`

```python
a_hat = self._model.sample_ode(x_tensor, ...)    # 内部调用 encode()
features = self._model.encode(x_tensor)           # 第二次调用 encode()
cls_logits = self._model.classify(features)
```

`sample_ode()` 内部已调用 `encode()`，然后 `_nn_infer()` 又显式调用一次。骨干（6 层因果空洞卷积，~595k 参数）每步运行两次，推理延迟翻倍。

**修复**：调用 `encode()` 一次，然后新增 `sample_ode_from_features(features, ...)` 方法复用特征，或重构 `sample_ode()` 接受可选预计算特征参数。

---

## 2. 检测模型与训练

### 2.1 架构设计问题

#### H1. mean pooling 丢失全部时序结构

**位置**：`detector/cfm_detector.py, classify()`

```python
pooled = cls_feat.mean(dim=1)  # (B, 64) — 100步信息坍缩为一个标量均值
```

攻击类型本质差异在于时序特征：A2 正弦有周期性，A4 阶跃有突变边缘，A5 重放有关联结构，A6 丢失有间歇 on/off 模式，A8 冻结有恒定特征。均值池化抹除了所有这些信息。64 维瓶颈下分类器仅靠"平均创新幅度"区分攻击。

**改进方向**：
- 注意力池化：可学习 query vector 对 100 步做交叉注意力
- 统计池化：拼接 `[mean, std, max, min]` 保留分布信息
- 轻量时序建模：1-2 层 GRU 后接线性分类器

#### H2. 正交约束仅在权重层面

**位置**：`detector/cfm_detector.py, OrthogonalFeatureSplitter.compute_ortho_loss()`

```python
cross = W_cls @ W_fm.T    # (64, 64) — 仅约束投影矩阵的行正交
return (cross ** 2).sum()
```

权重行正交不保证投影后的**特征**不相关——若骨干特征有强主方向，两个子空间的特征仍可能高度相关。且梯度冲突通过共享骨干传播，正交损失对骨干参数不生效，分类和回归的梯度仍会在骨干层冲突。

**改进方向**：
- 特征级正交：批内计算 `||Cov(cls_feat, fm_feat)||²`
- PCGrad：反向传播时投影冲突梯度
- Kendall 不确定性加权：每个任务学习自适应 loss weight

#### H3. A0 降权（0.15）削弱正常数据流匹配

**位置**：`detector/train_cfm.py:160, 225-229`

A0 攻击真值为零。速度目标 `v_target = -x_0`（即噪声负值），MSE 有不可约下限 ~1.0。将 A0 降权 6.7 倍：流匹配头对正常数据可能产生非零估计（虚警），t≈1 时速度场对 A0 欠约束，双头互补性被削弱。

**改进方向**：保留 class-balanced sampler，取消 A0 FM 降权，改为对 A0 输出添加 L1 正则 `||a_hat_A0||₁`；或使用与 `t` 成比例的权重。

#### H4. 训练与推理的积分器不一致（Euler vs RK4）

**位置**：`cfm_detector.py:kinematic_step_batch()` (RK4) vs. `cfm_backend.py:_kinematic_step()` (Euler)

训练时物理残差用 RK4，推理时创新计算用 Euler。单步误差 ~6.6×10⁻⁴m（低于传感器噪声），但分布偏移存在。A5（重放攻击）推理侧完全依赖 Euler 死推算，此处偏移最显著。

**改进方向**：统一为 Euler（Ts=0.05s 下误差 < 噪声水平），或统一为 RK4。

#### H5. ODE 求解仅 10 步 Euler

**位置**：`detector/cfm_detector.py, sample_ode()`

10 步 Euler（dt=0.1）：局部截断误差 O(0.01)，全局误差 O(0.1)。流匹配标准做法是 50-100 步。粗步长导致重建攻击信号精度受限。

**改进方向**：
- 中点法（RK2）：不增加函数评估次数，精度升至 O(dt³)
- 或探索一致性模型（单步生成）——对实时检测大幅降低延迟
- DPM-Solver 自适应步长

---

### 2.2 架构细节问题（MEDIUM）

#### M1. 时间嵌入 84% 频率对 t∈[0,1] 无效

**位置**：`detector/cfm_detector.py, SinusoidalTimeEmbedding`

Transformer 位置编码 `ω_i = 1/10000^(2i/d)` 为序列位置设计。对 t∈[0,1]：最快频率（i=0）周期 ~6.28，仅 0.16 周期；最慢频率（i=63）周期 ~5.5×10⁵，本质恒值。仅 ~10 对频率产生有意义变化，剩余 54 对浪费。

**改进**：将频率范围缩放为 `ω_i = 1/(1000^(2i/d))`，使所有频率在 [0,1] 内有效。

#### M2. 流匹配先验 N(0,I) 与攻击信号低维流形错配

攻击信号内在维度远低于 100×3=300：A1 ~3 维，A2 ~2 维，A4 ~2 维。从各向同性高维高斯输运到低维流形需要"压缩"，流路径可能弯曲，数值更难。

**改进方向**：随机插值使用数据依赖先验；或低维潜在流（16 维潜在空间做流匹配，解码到 300 维）。

#### M3. 高膨胀层采样极稀疏

膨胀=32 时卷积核仅采样 3 个点，覆盖 64 步中的 ~4.7%。若攻击起始恰在采样间隔内，该层完全错过。

**改进方向**：增加中等膨胀率 `[1,2,3,4,6,8,12,16,24,32]`；或在顶层添加轻量注意力补充长程依赖。

#### M4. 条件融合仅有简单加法

**位置**：`cfm_detector.py, FlowMatchingHead.forward()`

```python
h = h + cond   # 所有条件维度在所有流时间 t 上等权融合
```

t 早期（流状态是噪声）模型应更依赖条件信号；t 晚期（流状态包含信号）条件权重应降低。当前设计无法自适应调节。

**改进方向**：交叉注意力（流状态查询条件特征），或 FiLM（条件生成 scale/bias 调制流特征）。

#### M5. 推理窗口填充期 5 秒完全盲区

启动后需 100 步（5 秒）填满窗口，期间零检测能力。

**改进方向**：增长窗口策略——零填充或首帧复制，允许从第 1 步开始检测（准确率递减但非零）。

#### M6. 无攻击估计幅度合理性检查

**位置**：`cfm_backend.py, _recover()`

```python
y_rec = y_meas - nn_attack_est   # 盲减，无任何校验
```

若模型产出异常大值（OOD 输入/数值不稳定），恢复信号直接破坏 EKF。

**改进**：添加 `|nn_attack_est| > 3σ_train` 的幅度检查，超限回退至死推算或告警。

#### M7. 跨窗口无时序平滑

每次推理只用 `attack_seq[-1,:]`，丢弃前 99 步重建信息。连续调用间无 EMA/滤波，攻击估计可能步间不连贯。

**改进**：对攻击估计序列做 EMA 平滑，或重叠相加利用完整窗口输出。

#### M8. Kaiming 初始化用 relu 增益但激活是 GELU

**位置**：`cfm_detector.py, CausalDilatedConvBlock._init_weights()`

```python
nn.init.kaiming_normal_(conv.weight, mode='fan_out', nonlinearity='relu')
```

但激活使用 `F.gelu()`。GELU 的方差传播特性不同。6 层堆叠下小幅偏差可能累积。

**改进**：改用 `nonlinearity='linear'` (gain=1) 或直接使用 Xavier 均匀初始化。

#### M9. forward() 使用无效的 t=1, x=0 捷径

**位置**：`cfm_detector.py, CFMDetector.forward()`

```python
t_eval = torch.ones(B)           # t=1.0
x_t_eval = torch.zeros(...)      # x=0，但OT路径上 t=1 时 x 应等于目标信号
attack_seq = self.flow_head(t_eval, x_t_eval, fm_features)
```

模型从未在 `(t=1, x=0)` 上训练过——该点位于训练流形之外。`forward()` 的第三个返回值不可靠（`sample_ode()` 才是正确推理路径）。

**改进**：forward() 中移除 attack_seq 或改为内部调用 sample_ode()。

---

### 2.3 训练策略问题（LOW）

#### L1. OneCycleLR 预热期仅 12%

`pct_start=0.12` 意味着 LR 在 epoch 18/150 即达峰，剩余 88% 训练在衰减期。多任务损失下模型可能在子优局部极小值收敛。

**改进**：增加 `pct_start` 到 0.3-0.4，或使用 warmup-hold-decay。

#### L2. 损失权重固定，无自适应平衡

分类、流匹配、物理损失的收敛速度和噪声特性不同，固定权重使某一损失可能长期主导梯度。

**改进方向**：Kendall 不确定性加权（每损失一个可学习 log-σ），或 GradNorm 平衡梯度幅度。

#### L3. MC Dropout 已实现但从未使用

`enable_mc_dropout()` 存在但后端从不调用。置信度仅靠 softmax（已知神经网络对其过度自信）。

**改进**：运行 5-10 次 MC Dropout 前向，用分类方差作为额外不确定性信号。

#### L4. WeightedRandomSampler 带放回导致稀有类过拟合

少数类样本（A5, A6, A7, A8）每 epoch 出现多次，150 epoch 后同一窗口被学习数百次。

**改进**：对少数类添加类别感知数据增强（攻击参数微扰噪声）。

---

## 3. 仿真系统与控制

### 3.1 仿真正确性问题

#### H6. 位置硬限幅引入人为恢复

**位置**：`model.py:320-321`

```python
self.state[0] = np.clip(self.state[0], -2.5, 2.5)
self.state[1] = np.clip(self.state[1], -2.5, 2.5)
```

机器人被静默"传送"回边界内，现实中不可能。攻击导致跟踪丢失漂向边界时，clip 人为限制误差。

**修复**：移除位置 clip。NMPC 状态约束 `|x_e|,|y_e| ≤ 2.5m` 应足以保持边界。

#### H7. 仿真时间声明与实现不一致

**位置**：`model.py:14 (SIM_TIME=50.0, SIM_STEPS=1000)`, `simulate.py:41 (SIM_TIME=35.0)`

`simulate.py` 声明 `SIM_TIME = 35.0` 但导入 `model.SIM_STEPS = 1000`，实际运行 50 秒。设计文档声称 35s/700 步。三处不一致。

**修复**：统一为 50s，清理所有过时注释。

#### H9. 数据集多样性不足（仅 60 轨迹配置）

**位置**：`generate_dataset.py:576 (num_per_family=12)`

60 配置 × 9 攻击 = 540 运行。攻击参数固定时模型见到的攻击信号多样性极低。

**改进**：至少每家族 20-30 配置（100-150 总）。

---

### 3.2 模型一致性问题

#### H10. EKF 用 Euler，机器人用 RK4

**位置**：`model.py:464 (EKF predict)` vs. `model.py:284-306 (WMRKinematics.rk4_step)`

确定性模型不匹配被吸收进过程噪声 Q，使 Q 在物理上不可解释。

**改进**：统一为 Euler（Ts=0.05s 下误差 < 传感器噪声）。

---

### 3.3 攻击建模问题（MEDIUM）

#### M10. alpha=0.17 的物理推导缺失

`alpha = v_max / w_max = 0.3/1.76 = 0.17`。TurtleBot4 官方轮距 L=0.312m，`alpha = L/2 = 0.156 ≈ 0.17` 合理但未推导。

**改进**：添加 `alpha = L/2` 显式推导并引用 TurtleBot4 规格。

#### M11. RK4 在运动学模型中不必要

Ts=0.05s 下 Euler 位置误差 ~6.6×10⁻⁴m/步 < 传感器噪声 0.008m。RK4 增加 CasADi 符号编译开销和 IPOPT 求解时间，无实际精度收益。

**改进**：全系统统一为 Euler。

#### M12. A5 重放含启动阶段特征

重放缓冲区从 t=0 开始记录，包含 EKF 收敛阶段的较大跟踪误差。重放期间注入带这些大误差特征的数据，A5 可能人为易检测。

**改进**：添加 `replay_record_gap` 跳过前 N 秒记录，或随机选择记录窗口。

#### M13. A7 缩放含符号翻转——复合攻击

`scale_theta = -0.5` 翻转航向符号。A7 变为缩放+符号翻转的复合攻击，弱化类型定义。若攻击者仅用 `scale=0.5`（不翻转），检测器可能误判。

**改进**：限制缩放因子为正值，或随机化符号使检测器学习倍数模式而非符号模式。

#### M14. 攻击起始/持续时间范围不足

最短攻击 5s 仅比 NN 窗口（100 步=5s）稍长。慢速攻击（A3）在短持续时间内特征不明显。起始时间 [5,35]s 使部分攻击在仿真末期开始，观测时间不足。

**改进**：最小攻击时长提高至 10s（2×窗口长度）；对起始时间分层确保各阶段均有覆盖。

#### M15. Normalizer 分布偏移风险

RobustScaler 在训练集上计算 median/IQR。若某攻击类型创新值超出训练分布，测试集归一化产生极端值。

**改进**：验证测试集归一化特征在合理范围。或改用基于物理界的固定归一化（创新 /（传感器噪声 + 最大攻击幅度））。

---

## 4. 改进路线图

### 第一阶段：修复基础正确性

| 优先级 | 任务 | 影响 |
|--------|------|------|
| 🔴 | 修正 EKF R 矩阵 → 方差，重新调谐 Q | C1 |
| 🔴 | 物理损失改为对 ODE 终点或 t 加权 | C2 |
| 🔴 | 消除推理时骨干重复计算 | C3 |
| 🟡 | 统一 EKF 和机器人使用相同积分器（Euler） | H10 |
| 🟡 | 移除位置硬限幅 | H6 |
| 🟡 | 统一仿真时间为 50s | H7 |

### 第二阶段：架构升级

| 优先级 | 任务 | 影响 |
|--------|------|------|
| 🟡 | 注意力池化替代 mean pooling | H1 |
| 🟡 | 特征级正交约束（批内协方差惩罚或 PCGrad） | H2 |
| 🟡 | 取消 A0 FM 降权 → 输出 L1 正则化替代 | H3 |
| 🟡 | 时间嵌入频率范围缩放 | M1 |
| 🟡 | ODE 求解升级（RK2 或 DPM-Solver） | H5 |
| 🟢 | 增长窗口策略消除 5s 盲区 | M5 |
| 🟢 | 攻击估计幅度合理性检查 | M6 |
| 🟢 | EMA 跨窗口时序平滑 | M7 |
| 🟢 | 条件融合改为交叉注意力或 FiLM | M4 |

### 第三阶段：增强物理真实感

| 优先级 | 任务 | 影响 |
|--------|------|------|
| 🟡 | 增加轨迹配置至每家族 20-30 个 | H9 |
| 🟡 | 全系统统一 Euler 积分（移除 RK4） | M11 |
| 🟢 | A5 重放缓冲区跳过启动阶段 | M12 |
| 🟢 | A7 缩放因子随机化（含符号） | M13 |
| 🟢 | 攻击起始/持续时间分层，最小 10s | M14 |
| 🟢 | 添加 alpha = L/2 显式推导 | M10 |

---

## 附录：问题总表

### 检测模型与训练（16 项）

| # | 问题 | 严重度 |
|---|------|--------|
| C2 | 物理损失作用于含噪中间态 x_t | CRITICAL |
| C3 | 推理骨干运行两次 | CRITICAL |
| H1 | mean pooling 丢失时序结构 | HIGH |
| H2 | 正交约束仅权重级非特征级 | HIGH |
| H3 | A0 FM 降权 0.15 削弱正常流匹配 | HIGH |
| H4 | Euler/RK4 训练-推理不一致 | HIGH |
| H5 | ODE 10 步 Euler 精度不足 | HIGH |
| M1 | 时间嵌入 84% 频率无效 | MEDIUM |
| M2 | N(0,I) 先验与攻击信号低维流形错配 | MEDIUM |
| M3 | 高膨胀层采样极稀疏 | MEDIUM |
| M4 | 条件融合仅简单加法 | MEDIUM |
| M5 | 5 秒窗口填充盲区 | MEDIUM |
| M6 | 无攻击估计幅度检查 | MEDIUM |
| M7 | 跨窗口无时序平滑 | MEDIUM |
| M8 | Kaiming+relu 增益但用 GELU | MEDIUM |
| M9 | forward() t=1,x=0 无效捷径 | MEDIUM |

### 仿真系统与控制（10 项）

| # | 问题 | 严重度 |
|---|------|--------|
| C1 | EKF R=σ（非 σ²），差 125 倍 | CRITICAL |
| H6 | 位置硬限幅人为恢复 | HIGH |
| H7 | 仿真时间 35s vs 50s 不一致 | HIGH |
| H9 | 数据集仅 60 配置多样性不足 | HIGH |
| H10 | EKF Euler vs Robot RK4 不一致 | HIGH |
| M10 | alpha 推导缺失 | MEDIUM |
| M11 | RK4 过度工程 | MEDIUM |
| M12 | A5 重放含启动阶段特征 | MEDIUM |
| M13 | A7 缩放+符号翻转复合攻击 | MEDIUM |
| M14 | 攻击起始/持续时间范围不足 | MEDIUM |

### 训练策略（4 项）

| # | 问题 | 严重度 |
|---|------|--------|
| L1 | OneCycleLR 预热仅 12% | LOW |
| L2 | 损失权重固定无自适应 | LOW |
| L3 | MC Dropout 实现但未使用 | LOW |
| L4 | WeightedRandomSampler 带放回过拟合 | LOW |

---

> 总计：**30 项核心问题**（CRITICAL: 3, HIGH: 9, MEDIUM: 14, LOW: 4）
>
> 生成日期：2026-06-12
