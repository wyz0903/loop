# 轮式移动机器人抗传感器攻击仿真系统 — 设计说明文档

> 最后更新: 2026-06-10
> 版本: v4.0 — PINN-Flow 条件流匹配检测器 (CFMDetector)

---

## 1. 研究背景与目标

### 1.1 问题陈述

轮式移动机器人（WMR）在轨迹跟踪任务中，传感器通道可能遭受恶意攻击——攻击者向传感器测量值注入虚假信号 $a(k)$：

$$y_{meas}(k) = X_{true}(k) + n(k) + a(k)$$

其中 $n(k) \sim \mathcal{N}(0, \Sigma)$ 为正常传感器噪声。

### 1.2 研究目标

训练条件流匹配模型从传感器读数中**实时检测攻击类型并估计攻击信号**，在 EKF 之前完成信号恢复，从而恢复轨迹跟踪精度。

### 1.3 物理平台

TurtleBot4 安全模式，物理参数见第 3 节。

---

## 2. 系统架构

### 2.1 信号流（CFMDetector 方案）

```
ReferenceTrajectory → NMPCController → u_cmd → WMRKinematics → X_true
                                │         │            ↓
                                │         │ SensorSimulator (+noise)
                                │         │            ↓
                                │         │ SensorAttack (+a(k))
                                │         │            ↓
                                │         │        y_meas
                                │         │            ↓
                                │    ┌────┤── CFMDetector ──┤
                                │    │    │  滑动窗口缓冲区    │
                                │    │    │  Transformer主干  │
                                │    │    │  分类头+流匹配头   │
                                │    │    │  ODE采样→â(k)    │
                                │    │    └──────────────────┤
                                │    │                       ↓
                                │    └─────────── set_control(u_cmd)
                                ↓                       ↓
                              y_rec → EKFEstimator → X_hat → NMPCController
```

CFMDetector 对控制系统透明：输入 `y_meas`，输出 `y_rec`，不改动 EKF 或 NMPC 一行代码。
通过 `set_control(u_cmd)` 接收控制指令供内部运动学模型使用。

### 2.2 输入信号选择（5 通道）

| 通道 | 维度 | 物理含义 | 为什么需要 |
|------|------|---------|-----------|
| `internal_innovation` | 3 | 内部运动学新息 = y_meas − X_pred | 攻击的"异常信号"，内部运动学预测不含攻击信息 |
| `u_cmd` | 2 | 控制指令 [v, ω] | 提供运动上下文，区分"正常转弯导致的波动"和"攻击导致的异常" |

#### 输入信号设计原理

CFMDetector 维护**内部运动学模型**，用 $u_{cmd}$ 做开环位姿预测。内部新息定义为 $y_{meas} - X_{pred}$（$X_{pred}$ 由运动学积分产生，不含攻击信息），新息直接暴露攻击注入的信号分量。

这种设计使 CFMDetector **不依赖外部 EKF 内部状态**（不读取 EKF 的 $X_{pred}$ 或 $P$ 矩阵），完全通过自身运动学模型产生预测，实现即插即用。

---

## 3. 物理模型

### 3.1 WMR 运动学

两轮差速驱动机器人，以前端位姿 $\Upsilon_h = [x_h, y_h, \theta_h]^T$ 为控制目标：

$$\dot{\Upsilon}_h = F_h(\theta_h) \cdot u, \quad F_h = \begin{bmatrix} \cos\theta_h & -\alpha\sin\theta_h \\ \sin\theta_h & \alpha\cos\theta_h \\ 0 & 1 \end{bmatrix}$$

### 3.2 跟踪误差动力学（本体坐标系）

$$X_e = R(\theta_h)(\Upsilon_r - \Upsilon_h), \quad \dot{X}_e = f(X_e, u_r) + G(X_e) \cdot u$$

### 3.3 物理参数

| 符号 | 数值 | 单位 | 说明 |
|------|------|------|------|
| $\alpha$ | 0.17 | m | 前端偏移量 |
| $v_{max}$ | 0.3 | m/s | TurtleBot4 安全模式 |
| $\omega_{max}$ | 1.76 | rad/s | |
| $T_s$ | 0.05 | s | 仿真步长 |
| $T_{sim}$ | 35 | s | 仿真时长（700 步） |
| $t_{onset}$ | [5, 30] | s | 攻击开始时间（随机采样，A0 除外） |
| $X_0$ | $[0, 0.1, 0]^T$ | — | 初始状态 |
| $\sigma_{xy}$ | 0.008 | m | 传感器噪声（低噪声设置） |
| $\sigma_\theta$ | 0.004 | rad | 传感器噪声（低噪声设置） |

### 3.4 轨迹族（5 种）

| 族 | 采样概率 | 特征 |
|----|:---:|------|
| lissajous | 20% | 8字形光滑曲线，角速度正弦变化 |
| circular | 20% | 圆形轨迹，恒定线速度和角速度 |
| spiral | 20% | 阿基米德螺旋线，半径线性外扩，中心对称 |
| random_waypoint | 20% | 角速度方波切换（2-6s间隔），折线/锯齿形轨迹 |
| square | 20% | 正方形(圆角)轨迹，直行段+90°圆弧转弯，边长2-5m |

---

## 4. 攻击模型

### 4.1 攻击注入

$$y_{meas}(k) = X_{true}(k) + n(k) + a(k)$$

攻击作用于传感器通道，$n(k)$ 为正常传感器噪声，$a(k)$ 为攻击信号。

### 4.2 攻击类型（9 种）

| ID | 名称 | 信号特征 |
|:--:|------|---------|
| A0 | 正常 | $a(k) = 0$ |
| A1 | 恒定偏移 | $a = [0.15, -0.12, 0.10]^T$ 恒定 |
| A2 | 正弦 | $a = A\sin(2\pi ft)$, $A=0.12$, $f=0.8$Hz |
| A3 | 斜坡 | $a = rate \cdot (t - t_{onset})$ |
| A4 | 阶跃冲击 | $a = [0.25, -0.15, 0.18]^T$ 瞬时跳变 |
| A5 | 重放攻击 | 回放攻击前录制的传感器数据 (非加性) |
| A6 | 信号丢失 | Gilbert-Elliott Markov模型,故障态输出归零 (非加性) |
| A7 | 缩放攻击 | $y_{att} = \text{diag}(1.4, 0.6, -0.5) \cdot y_{clean}$ (唯一乘性) |
| A8 | 传感器冻结 | $y_{att}(t) = y_{clean}(t_{onset})$, 冻结在攻击时刻值 (非加性) |

---

## 5. PINN-Flow 条件流匹配检测系统

### 5.1 数据处理流水线

```
450 个 .npz 仿真文件（generate_dataset.py 生成, 50 configs × 9 attacks）
    │
    ▼ preprocess_data.py
    │  - 滑动窗口: 100 步 (5s), stride=1
    │  - 逐窗口标注: 攻击前窗口→A0, 攻击后窗口→对应攻击类型
    │  - 归一化: 特征通道→RobustScaler, u_cmd→物理上限
    │  - 保存: X_train.npy 等 .npy 文件
    │  - 支持两种划分: config (同分布) / trajectory (跨族泛化, circular→val)
    │
    ▼ train_cfm.py
       - 全内存加载 .npy 数据集
       - AMP FP16 混合精度 + CosineAnnealingLR
       - 训练 CFMDetector 网络
```

### 5.2 数据集统计

| 属性 | Train | Val | 总计 |
|------|-------|-----|------|
| 配置文件数 | 40（config 0-39） | 10（config 40-49） | 50 |
| 总窗口数 | 216,360 | 54,090 | 270,450 |
| A0（正常） | 88,360 (40.8%) | 22,090 (40.8%) | — |
| 每种攻击 | 16,000 (7.4%) | 4,000 (7.4%) | — |
| 窗口维度 | (100, 5) | (100, 5) | — |
| 重建目标 | (100, 3) | (100, 3) | — |

#### 标注规则

- A0 文件：所有 601 个窗口标注为 A0
- A1-A8 文件：窗口末尾 ≤ 步 300（攻击前）→ 标注 A0；否则 → 标注对应攻击类型
- 约 99 个窗口跨越攻击起点（含部分攻击），标注为攻击类型以支持尽早检测

### 5.3 归一化方案

| 通道 | 方法 | 参数来源 |
|------|------|---------|
| internal_innovation (ch 0-2) | RobustScaler: (x − median) / IQR | 训练集全局统计 |
| u_cmd (ch 3-4) | x / [v_max=0.3, ω_max=1.76] | 物理参数 |

选择 RobustScaler $(x - \text{median}) / \text{IQR}$ 归一化特征通道：对攻击产生的离群值鲁棒，且保留跨窗口的幅度可比性（不同攻击类型的幅度差异是重要分类线索）。u_cmd 通道用物理上限 $[v_{max}, \omega_{max}]$ 归一化：有明确物理边界，无需从数据学习。

### 5.4 模型架构（CFMDetector）

```
输入: (B, 100, 5)
  │
  ▼
共享 Transformer 主干 (4 层, d_model=128, 8 头):
  - 输入投影: Conv1d(5→128, k=1)
  - 可学习位置嵌入 (100, 128)
  - 4× TransformerEncoderLayer (d_model=128, nhead=8, dim_feedforward=512, dropout=0.1)
  - 输出: (B, 100, 128)
  │
  ├─ 分类头:
  │   AdaptiveAvgPool1d(1) → FC(128→256) → ReLU → Dropout(0.2)
  │   → FC(256→128) → ReLU → Dropout(0.1)
  │   → FC(128→9)
  │
  └─ 流匹配头 (AdaLN-Zero, 4 块):
      FC: cond(128+128) → scale, shift, gate (×4块 = 12个)
      每块: AdaLN(token) + MLP(时间嵌入128→dim_flow→128) + 零初始化门控
      输入: z_t (B,100,3), t (B,128), cond (B,100,128)
      输出: v_θ(t, z_t, cond) — 条件向量场 (B,100,3)
```

**参数量**：~1.2M

**架构创新**：
- **Transformer 主干**：全局注意力捕捉攻击信号的跨时间步依赖关系，优于 CNN 的局部感受野
- **AdaLN-Zero**：DiT 风格的零初始化自适应 LayerNorm，门控初始化为零 → 恒等初始化，训练稳定
- **条件流匹配**：学习从噪声到攻击信号的连续变换 v_θ(t, z_t)，ODE 采样生成高质量攻击估计

### 5.5 训练配置

| 参数 | 值 | 说明 |
|------|-----|------|
| Batch size | 2048 | 充分利用显存 |
| Workers | 2 | 多线程数据加载, prefetch=4 |
| Epochs | 150 | CosineAnnealingLR + 早停(50) |
| Optimizer | AdamW | lr=1e-3, wd=1e-4 |
| AMP | FP16 | 混合精度训练 |
| Loss | L_cls + λ_fm·L_fm + λ_phys·L_phys | 分类+流匹配+物理正则化 |
| λ_fm | 0.1 | 流匹配损失权重 |
| λ_phys | 0.05 | 物理正则化权重 |
| 标签平滑 | 0.05 | 正则化 |
| 类别平衡 | WeightedRandomSampler | 过采样少数类 |

### 5.6 损失函数设计

$$L = L_{cls} + \lambda_{fm} \cdot L_{fm} + \lambda_{phys} \cdot L_{phys}$$

- **L_cls**: 交叉熵分类损失（标签平滑 0.05）
- **L_fm**: MSE(v_θ, v_target)，其中 v_target = z_1 − z_0（OT 条件流匹配的目标向量场）
- **L_phys**: max(0, mean(||r_phys||²) − κ·Tr(R))，运动学 ODE 残差约束（PINN 正则化）

---

## 6. NMPC 控制器

### 6.1 优化问题

基于 CasADi + IPOPT：

$$\min_{U} \sum_{k=1}^{N} \left[ X_{k}^T Q X_{k} + \Delta u_k^T R \Delta u_k \right]$$

约束：非线性 RK4 误差动力学、菱形控制约束 $|v|/0.3 + |\omega|/1.76 \leq 1$、状态边界 $|x_e|,|y_e| \leq 2.5m$。

### 6.2 参数

| 参数 | 值 |
|------|-----|
| 预测步长 N | 20（1s） |
| Q | diag([0.1, 0.1, 0.01]) |
| R | diag([0.002, 0.002]) |
| IPOPT tol | 1e-4 |

---

## 7. EKF 状态估计

### 7.1 预测-更新循环

$$\text{Predict: } X_{pred} = \hat{X}_{k-1} + T_s F_h(\hat{\theta}) u_{cmd}, \quad P_{pred} = J P J^T + Q$$

$$\text{Update: } K = P_{pred}H^T(HP_{pred}H^T+R)^{-1}, \quad \hat{X} = X_{pred} + K(y_{rec} - HX_{pred})$$

### 7.2 协方差

| 矩阵 | 对角值 |
|------|--------|
| Q（过程噪声） | diag([5e-4, 5e-4, 5e-4]) |
| R（测量噪声） | diag([0.02, 0.02, 0.005]) |

---

## 8. CFMDetector — 即插即用攻击检测器

### 8.1 设计约束

CFMDetector 被设计为独立模块，对控制系统（EKF + NMPC）完全透明：
- **输入**：`y_meas`（传感器测量）+ `u_cmd`（控制指令，通过 `set_control()` 接收）
- **输出**：`y_rec`（恢复后的信号，直接替代 `y_meas` 送入 EKF）
- **不修改** EKF、NMPC 的任何参数或代码

### 8.2 内部架构

```
y_meas ──→ [内部运动学预测] ──→ 新息 ──→ [滑动窗口 100步] ──→ [CFMDetector]
                ↑                                                        ↓
            set_control(u_cmd)                              Transformer主干
                                                                    ↓
                                                    分类头 → 攻击类型 + 置信度
                                                    流匹配头 → â(k) (ODE采样)
                                                                    ↓
                                                              [信号恢复]
                                                    y_rec = y_meas − â(k)  → EKF
```

### 8.3 精简后处理（仅 2 项策略）

1. **ODE 采样**：从噪声 z_0 ~ N(0,I) 通过 Euler 10步积分获得 â(k)
2. **置信度阈值**：confidence < 0.5 → 直通不恢复；confidence ≥ 0.5 → y_rec = y_meas − â(k)

对比旧版 NNDetector 的 8 项后处理（EMA、多数投票、尾部加权平均、A5 迟滞、持久偏移检测、周期性重校准、输出裁剪），CFM 仅保留最核心的 2 项，架构更简洁。

### 8.4 检测器对比框架

| Tier | 名称 | 原理 |
|:----:|------|------|
| 0 | none | y_meas 直接入 EKF（最差性能基线） |
| 1 | cfm | CFMDetector — PINN-Flow 流匹配 + Transformer |
| 2 | oracle | 已知 a_true(k)，完美移除攻击（理论上界） |

---

## 9. 文件清单

| 文件 | 职责 |
|------|------|
| `model.py` | WMR 运动学 + 参考轨迹 + EKF + 传感器 |
| `controller.py` | CasADi NMPC 求解器 |
| `attack.py` | 9 种传感器攻击注入器 |
| `detector/cfm_detector.py` | CFMDetector 模型定义（Transformer + Flow Matching） |
| `detector/cfm_backend.py` | CFMDetectorBackend 推理包装器 |
| `detector/backend.py` | OracleDetector + DetectionResult + create_detector 工厂函数 |
| `generate_dataset.py` | 多样化轨迹攻击数据集生成器（450 个 .npz 文件，50 configs） |
| `preprocess_data.py` | 滑动窗口 + 归一化 + .npy 导出 |
| `train_cfm.py` | CFMDetector 训练脚本 |
| `evaluate.py` | 综合评估流水线 |
| `simulate.py` | 正常（无攻击）闭环仿真 |
| `simulate_detector.py` | 多级检测器对比仿真 |
| `analyze.py` | 结果分析与报告生成 |

---

## 10. 快速命令

```bash
# 环境
D:/anaconda/envs/learning&control/python.exe <script>.py
PY=D:/anaconda/envs/learning&control/python.exe

# 编译 NMPC 求解器
$PY controller.py

# 生成训练数据集（450 个仿真文件, 50 configs × 9 attacks）
$PY generate_dataset.py

# 预处理为神经网络输入（窗口 + 归一化 + .npy）
$PY preprocess_data.py --split-mode config
$PY preprocess_data.py --split-mode trajectory

# 训练 CFM 检测器
$PY train_cfm.py --epochs 150

# 仅评估
$PY train_cfm.py --eval-only models/cfm_cls_best.pt

# 综合评估
$PY evaluate.py --model-path models/cfm_cls_best.pt --output-dir eval/cfm_v1/

# 运行检测器对比仿真
$PY simulate_detector.py --attack A4 --tiers none,cfm,oracle
$PY simulate_detector.py --all --tiers none,cfm,oracle

# 模块自测
$PY model.py && $PY attack.py
```
