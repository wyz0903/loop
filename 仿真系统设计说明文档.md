# 轮式移动机器人抗传感器攻击仿真系统 — 设计说明文档

> 最后更新: 2026-06-19
> 版本: v7.0 — 运动学信任框架 + 在线 REINFORCE 策略学习
> 本文档在每次代码修改后必须同步更新，各模块状态标注说明：[稳定] = 已确定、论文级别功能；[开发中] = 正在迭代的实验性功能

---

## 0. 版本变更记录

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| v7.0 | 2026-06-19 | 重构微调框架为在线REINFORCE；冻结backbone(27K)作为特征提取器，REINFORCE在线学习信任权重策略(3.5K)；分类头每步监督CE更新(1K)；删除静态数据集微调模块(deploy/finetune/)；新增部署/在线学习目录(deploy/online/) |
| v6.0 | 2026-06-18 | 新增：可插拔恢复策略框架 (P0-P4)、监督闭环微调框架、系统消融实验; 更新参数量 (28K→64K)、训练损失 (cls + recon) |
| v5.0 | 2026-06-10 | 多尺度深度可分离卷积骨干 + 运动学一致性偏置注意力 + 物理引导解码器 |
| v4.0 | — | SimpleConvBackbone + 通道自注意力 (cls-only) |
| v3.0 | — | TCN 时序卷积骨干 |
| v2.0 | — | Transformer 编码器 (已删除) |

---

## 1. 研究背景与目标

### 1.1 问题陈述

轮式移动机器人（WMR）在轨迹跟踪任务中，传感器通道可能遭受恶意攻击——攻击者向传感器测量值注入虚假信号 $a(k)$：

$$y_{meas}(k) = X_{true}(k) + a(k)$$

（传感器噪声已关闭，简化问题聚焦攻击检测核心。）

### 1.2 研究目标

训练运动学感知检测器从传感器读数中**实时检测攻击类型并重建干净信号**，信号恢复后直接作为位姿估计送入NMPC，从而恢复轨迹跟踪精度。

### 1.3 当前研究问题

| # | 研究问题 | 方法 | 状态 |
|---|---------|------|------|
| 1 | 轻量攻击检测 | MultiScaleDSConvBackbone + 运动学一致性偏置注意力 | [稳定] |
| 2 | 物理引导信号重建 | PhysicsGuidedDecoder: y_pred = y_kin + delta_pred | [稳定] |
| 3 | 恢复策略设计 | 运动学信任框架: y_rec = α·y_meas + β·y_phys + γ·y_nn, 5种策略 | P0-P3 [稳定], P4 [开发中] |
| 4 | 闭环分布偏移量化 | 逐类退化诊断 + 监督 CE 在线分类头校准 | [开发中] |
| 5 | 在线恢复策略学习 | REINFORCE 策略梯度：冻结 backbone 作为特征提取器, 在线学习信任权重 π_φ(features, innov) → (α,β,γ)。分类头每步监督 CE 更新 | [开发中] |

### 1.4 物理平台

TurtleBot4 安全模式，物理参数见第 3 节。

---

## 2. 系统架构

### 2.1 信号流（检测器方案 + 运动学信任框架）

```
ReferenceTrajectory → NMPCController → u_cmd → WMRKinematics → X_true
                                │         │            ↓
                                │         │ SensorSimulator (+noise)
                                │         │            ↓
                                │         │ SensorAttack (+a(k))
                                │         │            ↓
                                │         │        y_meas
                                │         │            ↓
                                │    ┌────┤── 检测器 ───────────────────┤
                                │    │    │  滑动窗口缓冲区 100 步            │
                                │    │    │  innov_anchored (窗口级在线计算)   │
                                │    │    │  MultiScaleDSConvBackbone         │
                                │    │    │    ├→ 运动学引导注意力池化分类头    │
                                │    │    │    └→ PhysicsGuidedDecoder         │
                                │    │    │       y_pred = y_kin + delta_pred  │
                                │    │    └───────────────────────────────────┤
                                │    │                       ↓
                                │    │   RecoveryPolicy (运动学信任框架)      │
                                │    │   y_rec = α·y_meas + β·y_phys + γ·y_nn│
                                │    │   P0硬切换 / P1置信度融合 / P2门控     │
                                │    │   P3始终解码器 / P4自适应(开发中)      │
                                │    │                       ↓
                                │    └─────────── set_control(u_cmd)
                                ↓                       ↓
                              y_rec → 直接作为 X_hat → NMPCController
```

检测器对控制系统透明：输入 `y_meas`，输出 `y_rec`，不改动 NMPC 一行代码。
通过 `set_control(u_cmd)` 接收控制指令供内部运动学模型使用。

### 2.2 输入信号选择（8 通道）

| 通道 | 维度 | 物理含义 | 为什么需要 |
|------|------|---------|-----------|
| `y_meas` | 0:2 | 传感器测量值 [x, y, θ] | 原始观测（含攻击信号） |
| `innov_anchored` | 3:5 | 窗口锚定运动学新息 | y_meas − rollout(y_meas[0], u_cmd)，打破非加性攻击自指涉反馈环 |
| `u_cmd` | 6:7 | 控制指令 [v, ω] | 提供运动上下文 |

#### 输入信号设计原理

**窗口锚定运动学新息 (Unified Anchored Innovation)**：`innov[t] = y_meas[t] − rollout(y_meas[0], u_cmd[0:t])[t]`。
从窗口起点沿控制序列做运动学递推，与实测值逐帧比较。统一替代旧版 1-step innov + kin_res 双尺度，
打破非加性攻击（A4 重放/A5 丢包/A7 冻结）的自指涉污染反馈环。

---

## 3. 物理模型

### 3.1 WMR 运动学

两轮差速驱动机器人，以前端位姿 $\Upsilon_h = [x_h, y_h, \theta_h]^T$ 为控制目标：

$$\dot{\Upsilon}_h = F_h(\theta_h) \cdot u, \quad F_h = \begin{bmatrix} \cos\theta_h & -\alpha\sin\theta_h \\ \sin\theta_h & \alpha\cos\theta_h \\ 0 & 1 \end{bmatrix}$$

### 3.2 跟踪误差动力学（本体坐标系）

$$X_e = R(\theta_h)(\Upsilon_r - \Upsilon_h), \quad \dot{X}_e = f(X_e, u_r) + G(X_e) \cdot u$$

### 3.3 物理参数

| 符号 | 数值 | 单位 | 说明 |
|------|------|------|------|
| $\alpha$ | 0.17 | m | 前端偏移量 |
| $v_{max}$ | 0.3 | m/s | TurtleBot4 安全模式 |
| $\omega_{max}$ | 1.76 | rad/s | |
| $T_s$ | 0.05 | s | 仿真步长 |
| $T_{sim}$ | 50 | s | 仿真时长（1000 步） |
| $t_{onset}$ | [5, 30] | s | 攻击开始时间（随机采样，A0 除外） |
| $X_0$ | $[0, 0.1, 0]^T$ | — | 初始状态 |
| $\sigma_{xy}$ | 0.0 | m | 传感器噪声（关闭） |
| $\sigma_\theta$ | 0.0 | rad | 传感器噪声（关闭） |

### 3.4 轨迹族（5 种）

| 族 | 采样概率 | 特征 |
|----|:---:|------|
| lissajous | 20% | 8字形光滑曲线，角速度正弦变化 |
| circular | 20% | 圆形轨迹，恒定线速度和角速度 |
| spiral | 20% | 阿基米德螺旋线，半径线性外扩，中心对称 |
| random_waypoint | 20% | 角速度方波切换（2-6s间隔），折线/锯齿形轨迹 |
| square | 20% | 正方形(圆角)轨迹，直行段+90°圆弧转弯，边长2-5m |

---

## 4. 攻击模型

### 4.1 攻击注入

$$y_{meas}(k) = X_{true}(k) + a(k)$$

攻击作用于传感器通道，$a(k)$ 为攻击信号（传感器噪声已关闭）。

### 4.2 攻击类型（8 种）

| ID | 名称 | 信号特征 |
|:--:|------|---------|
| A0 | 正常 | $a(k) = 0$ |
| A1 | 恒定偏移 | $a = [0.15, -0.12, 0.10]^T$ 恒定 |
| A2 | 正弦 | $a = A\sin(2\pi ft)$, $A=0.12$, $f=0.8$Hz |
| A3 | 斜坡 | $a = rate \cdot (t - t_{onset})$ |
| A4 | 重放攻击 | 回放攻击前录制的传感器数据 (非加性) |
| A5 | 信号丢失 | Gilbert-Elliott Markov模型,故障态输出归零 (非加性) |
| A6 | 缩放攻击 | $y_{att} = \text{diag}(1.4, 0.6, -0.5) \cdot y_{clean}$ (唯一乘性) |
| A7 | 传感器冻结 | $y_{att}(t) = y_{clean}(t_{onset})$, 冻结在攻击时刻值 (非加性) |

---

## 5. 运动学感知检测系统 [稳定]

### 5.1 数据处理流水线

```
480 个 .npz 仿真文件（generate_dataset.py 生成, 60 configs × 8 attacks）
    │
    ▼ preprocess_data.py [稳定]
    │  - 滑动窗口: 100 步 (5s), stride=1
    │  - 窗口级计算: innov_anchored = y_meas − rollout(y_meas[0], u_cmd)
    │  - 逐窗口标注: 攻击信号活跃度检查 (L2 > 1e-6 且活跃步数 ≥ 5%)
    │  - 物理锚点归一化: y_meas/innov→工作空间边界, u_cmd→物理上限
    │  - 分层 IID 划分: 按轨迹族分层抽样 → train/val/test (70/15/15)
    │  - 保存: X_train.npy, Y_train_cls.npy, Y_train_clean.npy 至 dataset_win/
    │
    ▼ train.py [稳定]
       - 全内存加载 .npy 数据集
       - 联合训练: L = L_cls + λ_recon·L_recon
       - AdamW + ReduceLROnPlateau + 早停(50)
       - 前 warmup=20 epoch 仅分类损失
```

### 5.2 数据集统计

| 属性 | Train | Val | Test | 总计 |
|------|-------|-----|------|------|
| 配置文件数 | 42（每族8~9） | 9（每族1~2） | 9（每族1~2） | 60 |
| 总窗口数 | ~336k | ~72k | ~72k | ~480k |
| 每族分布在所有划分中 | ✓ | ✓ | ✓ | — |
| 窗口维度 | (100, 8) | (100, 8) | (100, 8) | — |
| 重建目标 | (100, 3) | (100, 3) | (100, 3) | — |

#### 标注规则

- A0 文件：所有窗口标注为 A0
- A1-A7 文件：窗口攻击活跃度检查 — 攻击信号 L2 范数 > 1e-6 且活跃步数 ≥ 5% → 标注对应攻击类型；否则 → A0

### 5.3 归一化方案（物理锚点归一化）

| 通道 | 方法 | 尺度锚点 |
|------|------|---------|
| y_meas (ch 0-2) | (x − median) / [2.5, 2.5, π] | 工作空间边界 |
| innov_anchored (ch 3-5) | (x − feat_median) / [2.5, 2.5, π] | y_meas 同物理空间 |
| u_cmd (ch 6-7) | x / [0.3, 1.76] | 控制物理上限 |

选择固定物理锚点尺度而非 IQR 归一化：IQR 可低至 1e-6 量级，将常规攻击信号放大至 10³-10⁵ 导致梯度爆炸。物理锚点数值天然稳定，物理含义清晰。

### 5.4 模型架构

```
输入: (B, 100, 8) [y_meas(3) + innov_anchored(3) + u_cmd(2)]
  │
  ▼
MultiScaleDSConvBackbone [稳定]:
  - 3 块膨胀深度可分离卷积, dilation=1,3,9
  - 每块: 3 并行深度卷积(k=7/5/3) → Pointwise混合 → BN+GELU → MaxPool
  - 通道: 8→32→64→96, 时序: 100→50→25→12
  - 参数量: ~27K
  │
  ├─ 运动学引导注意力池化分类头 [稳定]:
  │   scores = features @ query / √d + α·kin_bias
  │   → softmax → 加权求和 → LN → Dropout → Linear(96→8)
  │   kin_bias: 零参数运动学一致性偏置 (innov L2 范数 → 注意力抑制)
  │   ~1K 参数
  │
  └─ PhysicsGuidedDecoder [稳定]:
      ConvTranspose1d 上采样: 12→24→48→96→100 (通道 96→64→32→16→3)
      y_kin ← batch_kinematic_rollout(y0, u_cmd) [冻结梯度]
      y_pred = y_kin + delta_pred
      ~36K 参数
```

**总参数量**：**~64K**（骨干 ~27K + 分类头 ~1K + 解码器 ~36K）

**架构亮点**：
- **多尺度膨胀深度可分离卷积**：dilation=1,3,9 覆盖 0.35s/0.95s/2.75s 三个时间尺度，Pointwise Conv 隐式学习最优尺度加权（自适应 K）。46× 小于旧版 TCN (1.3M)，4× 小于 SimpleConvBackbone (~108K)
- **运动学一致性偏置注意力**：零参数物理先验 — 从 innov_anchored L2 范数计算注意力偏置。攻击步 (innov 大 → bias < 0) 被抑制，正常步获得更多关注
- **物理引导解码器**：仅学习攻击修正量 delta_pred，运动学递推由已知模型提供（冻结梯度），不浪费参数学习控制→状态映射

### 5.5 训练配置 [稳定]

| 参数 | 值 | 说明 |
|------|-----|------|
| Batch size | 256 | |
| Workers | 2 | 多线程数据加载, prefetch=4 |
| Epochs | 150 | ReduceLROnPlateau + 早停(50) |
| Optimizer | AdamW | lr=5e-4, wd=5e-4 |
| 精度 | FP32 | 标准精度训练 |
| Loss | L_cls + λ_recon·L_recon | 分类 + 重建 (epoch > warmup) |
| λ_recon | 0.3 | 重建损失权重 |
| Warmup | 20 epoch | 前 N epoch 仅分类 |
| Label Smoothing | 0.0 | 关闭 |
| A0 降采样 | 0.5/epoch | 每 epoch 随机降采样 50% A0 |
| ReduceLROnPlateau | patience=30, factor=0.8, min_lr=1e-6 | 学习率调度 |

### 5.6 损失函数设计

$$L = L_{cls} + \lambda_{recon} \cdot L_{recon} \quad (\text{epoch} > \text{warmup})$$

- **L_cls**: 交叉熵分类损失，8 类 softmax (A0-A7)，无标签平滑
- **L_recon**: MSE(y_pred, y_clean)，物理单位重建损失 (λ=0.3, warmup=20 epoch)

---

## 6. 运动学信任框架 — 恢复策略 [稳定] (P0-P3), [开发中] (P4)

### 6.1 设计动机

检测器输出攻击类型和置信度后，需要选择如何恢复传感器信号。最初是硬编码的硬切换策略，现在泛化为统一的**运动学信任框架**：

$$y_{rec} = \alpha(c, p) \cdot y_{meas} + \beta(c, p) \cdot \hat{y}_{phys} + \gamma(c, p) \cdot \hat{y}_{nn}$$

约束：$\alpha + \beta + \gamma = 1$, $\alpha,\beta,\gamma \in [0,1]$

三个信号源：
- **y_meas**：原始传感器测量（含攻击）
- **ŷ_phys**：运动学死推算（RK4一步预测，无攻击时接近真实）
- **ŷ_nn**：解码器重建（物理引导：y_kin + delta_pred，攻击修正后）

### 6.2 五种策略

| ID | 名称 | 权重规则 | 状态 |
|:--:|------|---------|:----:|
| P0 | HardSwitch | α=1(正常/低置信), β=1(攻击+无解码器), γ=1(攻击+解码器) | [稳定] |
| P1 | ConfidenceBlend | α=1-p, γ=p (攻击时), 置信度越高越信任解码器 | [稳定] |
| P2 | InnovationGated | g=exp(-‖innov‖²/σ²), g≈1→正常, g≈0→攻击, 门控调制α/γ | [稳定] |
| P3 | PhysicsFirst | 始终 (α,β,γ) = (0,0,1)，完全信任解码器重建 | [稳定] |
| P4 | AdaptiveTrust | 小MLP从编码器特征预测 (α,β,γ)，需在闭环数据上训练 | [开发中] |

### 6.3 设计约束

- 策略不访问检测器内部状态（窗口缓冲、NN 参数等）
- 策略是无状态的，或只通过接收到的 RecoveryContext 保留有限历史
- 所有超参数通过构造函数传入，便于序列化和复现
- 通过工厂函数和注册表管理，支持名称索引和动态创建

### 6.4 配置文件

```python
# deploy/recovery/config.py
@dataclass
class RecoveryConfig:
    policy: str = "hard_switch"           # p0-p4
    confidence_threshold: float = 0.5
    innov_sigma: float = 0.5              # P2: 门控带宽
    w_max: float = 0.95                   # P1: 最大融合权重
```

---

## 7. NMPC 控制器 [稳定]

### 7.1 优化问题

基于 CasADi + IPOPT：

$$\min_{U} \sum_{k=1}^{N} \left[ X_{k}^T Q X_{k} + \Delta u_k^T R \Delta u_k \right]$$

约束：非线性 RK4 误差动力学、菱形控制约束 $|v|/0.3 + |\omega|/1.76 \leq 1$、状态边界 $|x_e|,|y_e| \leq 2.5m$。

### 7.2 参数

| 参数 | 值 |
|------|-----|
| 预测步长 N | 20（1s） |
| Q | diag([0.1, 0.1, 0.01]) |
| R | diag([0.002, 0.002]) |
| IPOPT tol | 1e-4 |

---

## 8. 在线 REINFORCE 恢复策略学习 [开发中]

### 8.1 问题背景

恢复策略（P0-P3）是固定启发式规则，无法从闭环交互中学习。固定策略的问题：
- 不能适应检测器的分类错误（检测器将 A4 误判为 A0 时，策略仍信任传感器→灾难性后果）
- 不能针对不同轨迹族微调（spiral 需要更强的解码器信任，lissajous 可以更信任传感器）
- 不能从"自己做错→奖励低"的经验中改进

### 8.2 核心设计

**冻结 backbone（27K）作为特征提取器，在线学习两部分：**
1. 分类头（~1K）：每步监督 CE 更新（已知攻击标签→强信号，快速收敛）
2. 信任策略网络（~3.5K）：REINFORCE 策略梯度（运动学模型提供奖励→弱信号，慢速收敛）

**总可训练参数：~4.5K / 64K（7%），backbone 完全冻结。**

### 8.3 强化学习形式化

将闭环仿真建模为一个马尔可夫决策过程：

**状态 s_t**（策略看到了什么）：
```
s_t = [features_pooled(96) + confidence(1) + innov_norm(1) + cls_onehot(8)] = 106维
```
features 来自 backbone 输出的 (12,96) 全局平均池化。这些特征编码了"当前是否存在攻击、什么类型"的信息（backbone 原本就是被训练来做分类的）。

**动作 a_t**（策略能做什么）：
```
a_t ∈ {0, 1, 2}
  0 = 信任传感器 y_meas（α=1）
  1 = 信任死推算 y_phys（β=1）
  2 = 信任解码器 y_nn（γ=1）
```
策略输出 softmax 概率分布，按概率采样（探索）。评估时用 argmax（利用）。

**奖励 r_t**（策略做得好不好）：
```
r_t = -‖innov_{t+1}‖² - 0.1×‖tracking_error‖²
```
物理含义：如果选对了恢复方式，下一步的运动学一致性偏差应该小（innov ≈ 0），跟踪误差也应该小。运动学模型本身充当评判标准。

**状态转移**（环境动力学）：
```
s_t → [π_φ 输出 (α,β,γ)] → y_rec = α·y_meas + β·y_phys + γ·y_nn
  → NMPC 用 y_rec 算控制 u_t
  → WMR 运动学推进 → x_{t+1}
  → 传感器读数 y_{t+1}
  → 检测器提取新特征 → s_{t+1}
  → 计算 r_t（用 y_{t+1} 和 kinematic_predict(y_rec, u_t) 比较）
```

### 8.4 REINFORCE 算法

经典策略梯度算法（on-policy, Monte Carlo）：

$$φ ← φ + η · Σ_t (G_t - b) · ∇log π_φ(a_t | s_t)$$

其中 $G_t = r_t$（γ=0，即时 reward，因为 kinematics consistency 就是一步预测的即时反馈），$b$ 是历史 return 的 EMA（降方差），熵正则化项鼓励探索。

每 episode 子采样 128 个 transition（避免 901 个高度相关样本导致梯度爆炸），advantage 截断到 [-3, 3]。

### 8.5 分类头在线更新

不同于策略的 episode 级 REINFORCE，分类头采用**每步即时监督更新**：

$$θ_{cls} ← θ_{cls} - η_{cls} · ∇CE(cls_logits, true_label)$$

学习率 $η_{cls}=1e-5$ 远小于离线训练（$5e-4$），防止在线梯度爆炸。

### 8.6 双时间尺度稳定性

| 组件 | 学习速度 | 信号质量 | 收敛顺序 |
|------|---------|---------|---------|
| 分类头 | 快（每步 CE，强监督） | 标签100%正确 | 先收敛 |
| 策略网络 | 慢（128样本/集 REINFORCE） | reward 有噪声 | 后收敛 |

分类头先稳定 → 策略在稳定的特征/分类上下文上学习信任权重。温度退火（0.8→0.3）配合此设计：早期高温度让策略接近随机，分类头先跑稳；后期降温度让策略收敛。

### 8.7 组件架构

```
OnlineRunner [开发中]
  │
  ├─ TrustPolicyNetwork [开发中]
  │    MLP(106→32→3) + softmax → (α,β,γ)
  │    ~3.5K 参数
  │
  ├─ REINFORCETrainer [开发中]
  │    每 episode: 子采样128个transition → 重算log_prob → PG更新
  │    + 每步: 即时监督CE更新分类头
  │    baseline = EMA of episode returns
  │    advantage clipping [-3, 3]
  │
  └─ CheckpointManager [稳定] (deploy/tools/)
      策略网络版本化管理, 评估RMSE改善时自动保存
```

### 8.8 训练配置

| 参数 | 值 | 说明 |
|------|-----|------|
| lr (策略) | 3e-4 | REINFORCE 学习率 |
| lr (分类头) | 1e-5 | 每步监督更新学习率 |
| temperature | 0.8→0.3 | 探索-利用退火, decay=0.995/集 |
| entropy_coef | 0.02 | 鼓励探索, 防止过早收敛 |
| max_transitions_per_update | 128 | 子采样数, 防止相关样本梯度爆炸 |
| gamma | 0.0 | 无折扣（即时 reward） |
| episodes | 200 | 总训练 episode 数 |
| eval_every | 20 | 每 N 集评估一次 |
| attack_types | A1-A7 | 训练覆盖的攻击 |
| trajectory_types | 5族 | 训练覆盖的轨迹 |

### 8.9 与 SpecGuard 的关系

本方法与 SpecGuard (CCS'24, Dash et al.) 共享"使用 RL 学习传感器攻击恢复策略"的底层思想。关键差异：
- SpecGuard 使用 PPO 从头训练一个完整的恢复控制器；我们冻结预训练的 backbone 作为特征提取器，仅学习轻量信任权重策略（4.5K vs 完整控制器）
- SpecGuard 使用 STL 规范约束的复合奖励；我们使用运动学一致性（innov_anchored）作为奖励——已知物理模型作为免费评判
- SpecGuard 的 RecoveryChaining 针对一般 RAV 任务；我们的框架专为 WMR 轨迹跟踪设计

---

## 9. 系统实验 [开发中]

### 实验1: 固定策略消融（P0-P3）
4 种恢复策略 × 8 种攻击类型 × 1 族轨迹（lissajous）完整交叉对比。输出 RMSE 表、检测准确率汇总。**已完成初步结果：P2 InnovationGated 最优 (平均 RMSE 0.4138, Acc 62.5%)。**

### 实验2: 分布偏移诊断
逐攻击类型量化闭环中的检测准确率退化。**已完成初步结果：A1 恒偏是退化最严重的类别 (Acc 仅 6.9%)，A4/A7 有一定检测能力 (54-55%)。**

### 实验3: 在线 REINFORCE 学习
对比固定策略（P0-P3）vs 在线学习策略（P4 REINFORCE）。指标包括学习曲线（RMSE/Acc vs episode）、温度退火轨迹、熵变化。**训练中（200 episodes）。**

---

## 10. 状态估计

传感器直接观测全状态（$H = I$），恢复策略路由：

- **A0 / 低置信度 (<0.5)**：$y_{rec} = y_{meas}$（直通，避免注入估计误差）
- **检测到攻击 + 解码器可用**：$y_{rec} = y_{pred}[-1]$（解码器重建的最后一步）
- **检测到攻击 + 无解码器**：$y_{rec} = X_{pred}$（运动学死推算回退）

无检测器时，$\hat{X} = y_{meas}$（原始含噪测量）。估计值直接送入 NMPC 计算跟踪误差。

---

## 11. 检测器对比框架

| Tier | 名称 | 原理 | 状态 |
|:----:|------|------|:----:|
| 0 | none | y_meas 直送 NMPC（最差性能基线） | [稳定] |
| 1 | detector | 多尺度骨干 + 运动学偏置注意力 + 解码器 + 可插拔恢复策略 | [稳定] |
| 2 | oracle | 已知 a_true(k)，完美移除攻击（理论上界） | [稳定] |

---

## 12. 文件清单（含状态标注）

| 文件 | 职责 | 状态 |
|------|------|:----:|
| `model.py` | WMR 运动学 + 参考轨迹 + 传感器 | [稳定] |
| `controller/controller.py` | CasADi NMPC 求解器 | [稳定] |
| `attack.py` | 8 种传感器攻击注入器 | [稳定] |
| `detector/detector.py` | 检测模型定义（骨干 + 偏置 + 分类头 + 解码器） | [稳定] |
| `backend.py` | DetectorBackend 推理包装器（注：文件在项目根目录） | [稳定] |
| `generate_dataset.py` | 多样化轨迹攻击数据集生成器（480 个 .npz 文件） | [稳定] |
| `detector/preprocess_data.py` | 滑动窗口 + 归一化 + .npy 导出 | [稳定] |
| `detector/train.py` | 检测器训练脚本 (联合损失) | [稳定] |
| `detector/evaluate.py` | 测试集分类评估与可视化 | [稳定] |
| `simulate.py` | 闭环仿真（含恢复策略选择和对比） | [稳定] |
| `app/interactive_app.py` | tkinter 交互式 GUI | [稳定] |
| `app/plot_attack_demo.py` | 攻击演示图生成（论文用） | [稳定] |
| `app/plot_trajectory_coverage.py` | 五族轨迹空间覆盖范围图 | [稳定] |
| `deploy/recovery/` | 运动学信任框架：5种恢复策略(P0-P4) + 注册表 + 统一配置 | P0-P3[稳定], P4[开发中] |
| `deploy/online/` | 在线REINFORCE策略学习：TrustPolicyNetwork(3.5K) + REINFORCE训练器 + 在线仿真运行器 | [开发中] |
| `deploy/tools/` | Registry 注册表 + CheckpointManager | [稳定] |
| `tools/add_to_zotero.py` | Zotero 文献管理 | [开发中] |

---

## 13. 快速命令

```bash
# 环境
D:/anaconda/envs/learning&control/python.exe <script>.py
PY=D:/anaconda/envs/learning&control/python.exe

# 编译 NMPC 求解器
$PY controller.py

# 生成训练数据集（480 个仿真文件, 60 configs × 8 attacks）
$PY generate_dataset.py

# 预处理为神经网络输入（窗口 + 归一化 + 分层 IID 划分）
$PY preprocess_data.py

# 训练检测器（联合损失：分类 + 重建）
$PY detector/train.py --epochs 150

# 仅评估
$PY detector/train.py --eval-only detector/models/cfm_cls_best.pt

# 测试集分类评估
$PY detector/evaluate.py --model-path detector/models/cfm_cls_best.pt

# 运行闭环仿真（默认 CFM 模式）
$PY simulate.py --attack A1

# 指定恢复策略
$PY simulate.py --recovery-policy confidence_blend

# 横向对比所有恢复策略
$PY simulate.py --compare-strategies

# 逐类测试
$PY simulate.py --all

# 无检测器基线
$PY simulate.py --no-detector

# 在线 REINFORCE 训练 (冻结backbone, 学习恢复策略)
$PY -c "from deploy.online import OnlineRunner, OnlineConfig; from backend import DetectorBackend; runner = OnlineRunner(DetectorBackend(), OnlineConfig(n_episodes=200)); runner.train()"

# 模块自测
$PY model.py && $PY attack.py
```
